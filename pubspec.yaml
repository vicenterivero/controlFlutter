name: Control_vivir_o_morir
description: "A new Flutter project."
publish_to: 'none'

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/logo.jpg"

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  http: ^1.2.0
  mobile_scanner: ^3.5.7
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
